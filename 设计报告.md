# ESP32-S3 智能垃圾分类系统设计报告

---

## 1 绪论

### 1.1 项目设计的意义、背景及目的

#### 背景

随着我国城市化进程的加速和居民生活水平的不断提高，生活垃圾产生量持续快速增长。据住建部统计，我国每年产生生活垃圾超过 2.5 亿吨，且仍在以每年 5%-8% 的速度递增。垃圾分类作为垃圾减量化、资源化、无害化处理的关键环节，已成为城市管理的重要课题。2020 年以来，全国多个城市相继实施《生活垃圾管理条例》，强制推行垃圾分类制度，对垃圾投放的准确率提出了更高要求。

然而，在实际推行过程中，居民普遍面临"不知道怎么分"的困惑：不同城市分类标准存在差异、日常物品种类繁多、特殊物品归属模糊等问题导致分类准确率偏低。传统的社区督导员模式成本高、覆盖有限，难以持续。因此，研发一种能够自动识别垃圾物品并提示分类的智能系统，具有重要的现实意义。

#### 意义

本项目的意义在于：

1. **降低分类门槛**：用户无需记忆复杂的分类规则，只需将物品置于摄像头前，系统自动完成识别和分类提示，降低居民参与垃圾分类的学习成本。
2. **提高分类精度**：利用深度学习模型的视觉识别能力，可识别 180+ 种日常物品并映射到四类生活垃圾，识别准确率远超普通居民的自主判断。
3. **自动化交互**：通过 TOF 激光测距传感器实现无接触式自动触发，用户只需将物品靠近设备即可完成识别，适合社区垃圾投放点等公共场所使用。
4. **可扩展架构**：采用 ESP32-S3 + FastAPI 后端 + React 前端的分离式架构，支持多模型切换（本地 CLIP / 云端视觉大模型），便于后续升级和扩展。

#### 目的

本项目旨在设计并实现一套基于 ESP32-S3 微控制器的智能垃圾分类识别系统，实现以下目标：

1. 构建完整的物联网硬件终端，集成摄像头、激光测距、显示屏和 WiFi 通信模块。
2. 基于零样本图像分类技术（CLIP / 豆包向量模型），实现无需训练的通用物品识别与垃圾分类映射。
3. 设计 Web 端管理仪表板，提供实时监控、模型切换、参数配置和历史记录查询功能。
4. 实现距离感应自动触发拍照识别的全自动化工作流。

### 1.2 项目设计的主要工作内容

本项目围绕"硬件终端 + AI 识别 + Web 管理"三层架构展开，整体工作内容如下：

**硬件设计与实现**
- 选用 ESP32-S3-WROOM-1-N16R8 模组作为主控芯片（内置 16MB Flash 和 8MB PSRAM），搭载 OV3660 摄像头模组采集图像、TOF200C（VL53L0X）激光测距传感器实现距离触发、ST7735S 1.8 寸 TFT 屏幕显示识别结果。
- 设计各模块的电路连接方案，其中摄像头通过 FPC 接口连接，TOF 传感器通过 I2C 通信，屏幕采用软件 SPI 驱动。
- 完成固件程序开发，包括 WiFi 联网、摄像头驱动、TOF 测距、HTTP 通信、屏幕渲染等功能模块。

**AI 识别模型集成**
- 集成 OpenAI CLIP ViT-B/32 零样本图像分类模型（最终替换为豆包向量模型），实现本地快速分类推理。
- 设计两阶段分组分类架构：第一阶段在 16 个物品分组中确定最可能的物品类别组，第二阶段在组内进行细粒度分类，显著提升分类准确率。
- 集成豆包 Vision、通义千问 Vision 和兼容 OpenAI 格式的自定义视觉大模型作为可选的云端分类引擎。
- 集成 YOLO 目标检测模型作为网页端的辅助检测工具。

**后端服务开发**
- 基于 FastAPI 框架构建 RESTful API 后端服务，提供分类识别、模型管理、硬件状态监控、历史记录存储、触发配置等 20+ 个 API 端点。
- 实现多模型路由与自动回退机制，当云端视觉模型调用失败时自动回退到本地模型。
- 实现 SSE（Server-Sent Events）实时推送机制，ESP32 拍照后即时推送结果到前端。
- 实现 WebSocket 实时视频流推送，前端可实时查看摄像头画面。

**前端仪表板开发**
- 基于 React 18 + Vite 6 + Tailwind CSS 3 构建 Web 管理界面。
- 实现四个功能面板：按键测试面板、硬件与触发面板、识别引擎面板、上传与结果面板。
- 集成系统状态监控（服务状态、硬件在线、当前模型、触发模式、推理队列等）。
- 支持模型切换、API Key 配置、触发参数调节、历史记录浏览等功能。

**系统调试与优化**
- 经过多轮版本迭代（v1.0 → v5.3.0），持续优化系统稳定性、推理速度和用户体验。
- 硬件端优化：TCP 存活性检测、HTTP 重试机制、JPEG 质量调优、PSRAM 内存优化。
- 服务端优化：文本特征预计算（推理延迟降低 35-50%）、并行模型加载（启动时间减半）、推理信号量背压、锁拆分减少竞争。
- 前端优化：SSE 替代轮询、React.memo 按需渲染、自适应轮询间隔。

---

## 2 设计方案

### 2.1 总体设计思路

#### 设计思路

本系统的核心设计思路是构建一个"感知-识别-反馈"的闭环：

1. **感知层**（ESP32-S3 终端）：通过 TOF 激光测距传感器感知物体靠近，自动触发 OV3660 摄像头拍照，将 JPEG 图像通过 WiFi 上传至服务端。

2. **识别层**（FastAPI 后端）：接收图像后进行 AI 推理——利用 CLIP/豆包向量模型提取图像特征并与预编码的文本标签特征进行相似度匹配，通过两阶段分组分类确定物品类别和垃圾归属，返回结构化的分类结果。

3. **反馈层**（ESP32 屏幕 + Web 仪表板）：ESP32 收到结果后立即在 ST7735 屏幕上显示垃圾分类信息；同时服务端通过 SSE 向前端推送结果，Web 仪表板实时更新。

**关键设计决策：**
- 零样本分类方案：采用 CLIP 模型实现零样本分类，无需针对特定垃圾类别进行训练，泛化能力强，可灵活扩展标签。
- 两阶段分组分类：将 180+ 个标签按语义分为 16 组，先确定物品所属组别，再在组内细分类，降低类别间混淆。
- 触发配置远程化：ESP32 定期从服务器拉取触发参数（距离范围、缓冲时间、触发间隔等），前端修改即时生效，无需重新刷写固件。
- 多模型路由：前端可随时切换分类模型，ESP32 透明跟随，无需重新编译上传。

#### 系统整体设计框图

```
┌─────────────────────────────────────────────────────────────────┐
│                       整体系统架构图                             │
│                                                                 │
│  ┌──────────────┐     WiFi/HTTP      ┌──────────────┐          │
│  │   ESP32-S3   │ ──── classify ────▶│   FastAPI    │          │
│  │  ┌─────────┐ │                    │  ┌─────────┐ │          │
│  │  │OV3660   │ │                    │  │CLIP/豆包│ │          │
│  │  │摄像头    │ │◀─── JSON result ── │  │向量模型  │ │          │
│  │  │3MP      │ │                    │  │(本地)    │ │          │
│  │  ├─────────┤ │                    │  ├─────────┤ │          │
│  │  │TOF200C  │ │   GET /trigger/    │  │视觉大模型│ │          │
│  │  │激光测距  │ │   config (30s)    │  │(云端)    │ │          │
│  │  │I2C      │ │◀────────────────────│  ├─────────┤ │          │
│  │  ├─────────┤ │                    │  │YOLO检测 │ │          │
│  │  │ST7735S  │ │   GET /heartbeat   │  │(本地)    │ │          │
│  │  │1.8" TFT │ │   (30s)           │  └─────────┘ │          │
│  │  └─────────┘ │                    │  :8085       │          │
│  │              │                    │              │          │
│  └──────┬───────┘                    └──────┬───────┘          │
│         │                                  │                   │
│         │ POST /classify?source=esp32       │ SSE /events      │
│         │                                  │ WebSocket /ws    │
│         │                                  ▼                   │
│         │                         ┌──────────────┐            │
│         │                         │  React SPA   │            │
│         │                         │  Web 仪表板   │            │
│         │                         │  :5173/:8085 │            │
│         │                         │              │            │
│         │                         │  ┌─────────┐  │            │
│         │                         │  │硬件面板  │  │            │
│         │                         │  ├─────────┤  │            │
│         │                         │  │模型选择器│  │            │
│         │                         │  ├─────────┤  │            │
│         │                         │  │上传识别  │  │            │
│         │                         │  ├─────────┤  │            │
│         │                         │  │历史记录  │  │            │
│         │                         │  └─────────┘  │            │
│         │                         └──────────────┘            │
│         │                                                    │
│  ┌──────┴─────────────────────────────────────────────────────┤
│  │  通信协议：WiFi STA + HTTP multipart/form-data + JSON      │
│  │  实时推送：SSE (Server-Sent Events) / WebSocket            │
│  └────────────────────────────────────────────────────────────┘
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  识别流程：拍照 → YUV422 原生采集 → fmt2jpg 转 JPEG        ││
│  │  → HTTP 上传 → 图像预处理 → 特征提取 → 相似度匹配          ││
│  │  → 两阶段分组分类 → 垃圾分类映射 → 返回结果                ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 硬件设计

#### 核心硬件选型

| 组件 | 型号 | 说明 |
|------|------|------|
| 主控芯片 | ESP32-S3-WROOM-1-N16R8 | Xtensa LX7 双核 240MHz，16MB Flash + 8MB PSRAM |
| 摄像头 | OV3660 | 3MP 彩色图像传感器，FPC 接口 |
| 激光测距 | TOF200C (VL53L0X) | ST 产飞行时间测距传感器，I2C 接口，最大 2m |
| 显示屏 | ST7735S | 1.8 英寸 TFT 彩色 LCD，128×160 分辨率，SPI 接口 |

#### 电路连接原理图

**STM7735S 屏幕接线（软件 SPI）**

```
┌──────────────────────────────────────────────────┐
│  ST7735S 1.8" TFT         ESP32-S3              │
│                          ┌──────────┐           │
│  GND   ────────────────▶│  GND     │           │
│  VDD   ────────────────▶│  3.3V    │           │
│  SCL   ────────────────▶│  GPIO 14 │ (SPI CLK) │
│  SDA   ────────────────▶│  GPIO 38 │ (SPI MOSI)│
│  RST   ────────────────▶│  GPIO 1  │           │
│  DC    ────────────────▶│  GPIO 39 │           │
│  CS    ────────────────▶│  GPIO 40 │           │
│  BLK   ────────────────▶│  GPIO 41 │ (背光)    │
└──────────────────────────────────────────────────┘
```

**TOF200C 激光测距模块接线（I2C）**

```
┌──────────────────────────────────────────────────┐
│  TOF200C (VL53L0X)       ESP32-S3              │
│                          ┌──────────┐           │
│  VIN   ────────────────▶│  3.3V    │           │
│  GND   ────────────────▶│  GND     │           │
│  SDA   ────────────────▶│  GPIO 47 │ (I2C SDA) │
│  SCL   ────────────────▶│  GPIO 21 │ (I2C SCL) │
│  INT   ────────────────▶│  GPIO 48 │ (中断)    │
│  SHUT  ────────────────▶│  GPIO 45 │ (关断)    │
└──────────────────────────────────────────────────┘
```

**OV3660 摄像头接线**

```
┌──────────────────────────────────────────────────┐
│  OV3660 摄像头           ESP32-S3              │
│                          ┌──────────┐           │
│  PWDN  ────────────────▶│  -1 (NC) │ (不上电)  │
│  RESET ────────────────▶│  -1 (NC) │           │
│  XCLK  ────────────────▶│  GPIO 15 │ (时钟)    │
│  SIOD  ────────────────▶│  GPIO 4  │ (I2C SDA) │
│  SIOC  ────────────────▶│  GPIO 5  │ (I2C SCL) │
│  Y9-2  ────────────────▶│  GPIO 16/17/18/12    │
│  Y5-3  ────────────────▶│  GPIO 10/9/11        │
│  VSYNC ────────────────▶│  GPIO 6  │ (帧同步)  │
│  HREF  ────────────────▶│  GPIO 7  │ (行参考)  │
│  PCLK  ────────────────▶│  GPIO 13 │ (像素时钟)│
└──────────────────────────────────────────────────┘
```

**摄像头配置要点：**
- 像素格式采用 `PIXFORMAT_YUV422`（传感器原生色彩空间），避免 RGB565 字节序导致的颜色错误。
- 帧尺寸选择 `FRAMESIZE_QVGA`（320×240），平衡识别精度和传输速度。
- 帧缓冲设为 1（`fb_count=1`），减轻 PSRAM 碎片化。
- 图像数据存入 PSRAM（`CAMERA_FB_IN_PSRAM`），利用 8MB 大容量 RAM。
- 使用 `fmt2jpg` 在固件侧将 YUV422 转换为 JPEG 后再上传，上传体积降低 70-90%。

#### 硬件工作流程

1. 系统上电 → ESP32-S3 初始化外设（摄像头、TOF、屏幕）
2. WiFi 联网（支持 30 秒超时重试，显示扫描到的网络列表）
3. 首次从服务器拉取触发配置（距离范围、缓冲时间等）
4. 进入待机循环：
   - TOF 传感器每秒读取距离值，若检测到物体在设定范围内（默认 30-300mm）且稳定超过缓冲时间 → 触发拍照
   - 拍照后丢弃传感器缓冲区旧帧 → 采集新帧 → fmt2jpg 转 JPEG → WiFi 上传 → 接收 JSON 结果 → 屏幕显示分类信息
   - 同时维持 30 秒心跳上报和 8 秒 TCP 存活性探测

### 2.3 软件设计

#### 系统软件架构

本系统采用三层分离架构：

```
┌──────────────────────────────────────────────────────────────┐
│                     软件架构层次图                            │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           前端层 (React SPA)                        │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────┐  │    │
│  │  │硬件面板   │ │模型选择器 │ │上传面板  │ │历史记录│  │    │
│  │  │Hardware   │ │Model     │ │Upload    │ │History │  │    │
│  │  │Panel      │ │Selector  │ │Panel     │ │List    │  │    │
│  │  └──────────┘ └──────────┘ └──────────┘ └───────┘  │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           │    │
│  │  │按键测试   │ │结果浮层   │ │置信度仪表 │           │    │
│  │  │Trigger   │ │Result    │ │Confidence│           │    │
│  │  │Panel     │ │Overlay   │ │Gauge     │           │    │
│  │  └──────────┘ └──────────┘ └──────────┘           │    │
│  └─────────────────────────────────────────────────────┘    │
│                         │ REST API / SSE / WS               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           服务层 (FastAPI Server)                   │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           │    │
│  │  │分类识别   │ │模型路由   │ │硬件管理  │           │    │
│  │  │/classify │ │/model/*  │ │/hardware/*│          │    │
│  │  ├──────────┤ ├──────────┤ ├──────────┤           │    │
│  │  │历史记录   │ │触发配置   │ │SSE推送   │           │    │
│  │  │/history  │ │/trigger/*│ │/events   │           │    │
│  │  └──────────┘ └──────────┘ └──────────┘           │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │  CLIP模型 / 豆包向量模型 / 视觉大模型    │     │    │
│  │  │   YOLO检测  │  文本特征预计算  │ 多线程池 │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  └─────────────────────────────────────────────────────┘    │
│                         WiFi / HTTP                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │    固件层 (ESP32-S3 Arduino C++)                    │    │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           │    │
│  │  │摄像头驱动 │ │TOF测距   │ │屏幕渲染  │           │    │
│  │  │esp_camera│ │VL53L0X   │ │ST7735    │           │    │
│  │  ├──────────┤ ├──────────┤ ├──────────┤           │    │
│  │  │HTTP通信  │ │配置同步   │ │TCP存活   │           │    │
│  │  │WiFiClient│ │/trigger  │ │探测      │           │    │
│  │  └──────────┘ └──────────┘ └──────────┘           │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

#### 固件程序流程

**主程序流程图：**

```
┌──────────────────┐
│     系统启动      │
│   ESP32-S3 上电   │
└────────┬─────────┘
         ▼
┌──────────────────┐
│  外设初始化       │
│ ┌ 摄像头 OV3660   │
│ ├ TOF200C 测距    │
│ └ ST7735 屏幕     │
└────────┬─────────┘
         ▼
┌──────────────────┐
│  WiFi 联网       │
│ (扫描→连接→显示IP)│
│ (超时30s, 60次重试)│
└────────┬─────────┘
         ▼
┌──────────────────┐
│  拉取触发配置      │
│  GET /trigger/    │
│  config           │
└────────┬─────────┘
         ▼
┌──────────────────────────────────────────────┐
│              主循环 loop()                    │
│                                              │
│  ┌─────────────┐   ┌──────────────┐         │
│  │ TCP 存活性   │   │ 30s 定时：    │         │
│  │ 探测 (8s)    │   │ 拉取触发配置   │         │
│  └──────┬──────┘   │ + 发送心跳    │         │
│         │          └──────┬───────┘         │
│         ▼                 ▼                  │
│  ┌─────────────────────────────────┐        │
│  │  TOF 距离检测 (每轮循环)        │        │
│  │  读取 VL53L0X 距离值           │        │
│  │  判断是否在触发范围内          │        │
│  │  若物体稳定超过缓冲时间 → 触发 │        │
│  └──────────────┬──────────────────┘        │
│                 ▼                            │
│  ┌─────────────────────────────────┐        │
│  │  captureAndClassify()           │        │
│  │  ┌ 丢弃旧帧 → 拍照               │        │
│  │  ├ YUV422 → fmt2jpg 转 JPEG     │        │
│  │  ├ HTTP POST multipart 上传      │        │
│  │  │  (最多3次重试, 总超时30s)     │        │
│  │  ├ 解析 JSON 响应               │        │
│  │  └ 屏幕显示分类结果 (保持12s)    │        │
│  └─────────────────────────────────┘        │
│                 ▼                            │
│  ┌─────────────────────────────────┐        │
│  │  返回待机状态                     │        │
│  │  等待下一次触发                  │        │
│  └─────────────────────────────────┘        │
└──────────────────────────────────────────────┘
```

**核心函数说明：**

| 函数 | 功能 | 关键实现 |
|------|------|----------|
| `wifiConnect()` | WiFi 连接 | 禁用省电模式（`WIFI_PS_NONE`），30 秒超时，屏幕显示扫描到的网络列表 |
| `cameraInit()` | 摄像头初始化 | YUV422 原生格式，QVGA 尺寸，单缓冲，PSRAM 存储 |
| `tofInit()` | TOF 传感器初始化 | I2C 400KHz 快速模式，SHUT 引脚控制 |
| `readTOF()` | 读取距离值 | 返回毫米级距离，异常返回 -1 |
| `fetchTriggerConfig()` | 拉取远程触发配置 | 每 30 秒同步，配置变化时屏幕显示反馈 |
| `sendHeartbeat()` | 心跳上报 | 携带 device_id/firmware_version/ip_address，30 秒间隔 |
| `probeServerLiveness()` | TCP 存活性探测 | 空闲时每 8 秒探测，解决 WiFi 已连但服务不可达的盲区 |
| `postMultipartJpeg()` | 上传 JPEG 图片 | multipart/form-data，3 次重试，总超时看门狗 30s |
| `captureAndClassify()` | 拍照+识别全流程 | 丢弃旧帧→拍照→转换→上传→解析→显示 |

#### 后端程序流程

**分类识别核心流程（两阶段分组分类）：**

```
                    ┌─────────────────┐
                    │  接收图像        │
                    │  POST /classify  │
                    └────────┬────────┘
                             ▼
                    ┌─────────────────┐
                    │  图像预处理       │
                    │  RGB 转换        │
                    └────────┬────────┘
                             ▼
                    ┌─────────────────┐
                    │  特征提取         │
                    │  CLIP/豆包向量模型 │
                    │  图像 → 特征向量  │
                    └────────┬────────┘
                             ▼
               ┌─────────────────────────┐
               │  与预计算文本特征做      │
               │  余弦相似度匹配          │
               │  (180+ 标签)            │
               └────────┬────────────────┘
                        ▼
        ┌──────────────────────────────────┐
        │  第一阶段：组级 Softmax          │
        │  16 组分别取组内最大 logit       │
        │  在组间做 softmax                │
        │  确定"胜出组"                    │
        └────────┬─────────────────────────┘
                 ▼
        ┌──────────────────────────────────┐
        │  是否需要跨组扩展？              │
        │  胜出组 vs 第二名组              │
        │  概率差 < 20% → 合并两组候选     │
        │  否则 → 仅胜出组                 │
        └────────┬─────────────────────────┘
                 ▼
        ┌──────────────────────────────────┐
        │  第二阶段：组内 Softmax          │
        │  候选组内标签做 softmax          │
        │  阈值 ≥ 20% → 确认结果          │
        │  < 20% → 返回"未知物品"         │
        └────────┬─────────────────────────┘
                 ▼
        ┌──────────────────────────────────┐
        │  垃圾分类映射                    │
        │  WASTE_CATEGORY_MAP 查表        │
        │  → 四类垃圾                      │
        │  → 返回 JSON 结果               │
        └──────────────────────────────────┘
```

**API 接口列表：**

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/classify` | 图像分类（支持 `?source=esp32` 标记来源） |
| POST | `/detect` | YOLO 目标检测，返回检测框 |
| GET | `/health` | 健康检查（运行时间/硬件在线/标签数/激活模型） |
| GET | `/models` | 可用模型信息（含视觉模型配置状态） |
| GET/POST | `/model/active` | 获取/切换当前激活的分类模型 |
| POST | `/model/config` | 配置视觉模型 API Key/Base URL/Model Name |
| GET | `/history` | 历史记录（分页，含缩略图链接） |
| DELETE | `/history` | 清空历史记录 |
| POST | `/hardware/capture` | ESP32 上传采集图片 |
| GET | `/hardware/image` | 获取最新硬件采集图片 |
| GET | `/hardware/status` | 硬件状态（在线/IP/采集次数/固件版本） |
| GET | `/hardware/heartbeat` | 硬件心跳上报 |
| GET/POST | `/trigger/config` | 获取/设置触发配置（距离/缓冲/间隔） |
| GET | `/events` | SSE 实时推送流 |
| GET | `/ws/video` | WebSocket 实时视频流 |
| GET | `/metrics/runtime` | 运行时指标（QPS/队列深度/推理统计） |
| GET | `/config/all` | 完整系统配置 |
| GET | `/camera/config` | 获取摄像头参数（亮度/对比度/饱和度等） |

**视觉大模型集成：**

后端同时支持三种云端视觉大模型作为可选分类引擎，与本地模型（CLIP/豆包向量模型）互补：

1. **豆包 Vision**：字节跳动豆包视觉大模型，通过火山引擎 API 调用，需配置 `DOUBAO_API_KEY`
2. **千问 Vision**：阿里通义千问视觉大模型，通过 DashScope API 调用，需配置 `DASHSCOPE_API_KEY`
3. **自定义 Vision**：兼容 OpenAI 格式的任意视觉模型（如本地部署的 LLaVA、Qwen-VL 等）

模型路由策略：前端设置激活模型 → ESP32 始终调用 `/classify` → 后端按激活模型路由。若云端模型未配置或调用失败，自动回退到本地模型，ESP32 和用户透明。

#### 前端程序流程

**前端架构与数据流：**

```
┌─────────────────────────────────────────────┐
│           React SPA 数据流图                 │
│                                             │
│  ┌─────────┐    ┌──────────────┐           │
│  │ usePolling│──▶│   api.js    │─── REST ──▶│  FastAPI
│  │ 5s health │   │  聚合层      │           │  后端
│  │ 10s metrics│   └──────┬───────┘           │
│  │ 60s hwStatus│         │                   │
│  └─────────┘            │                   │
│                         │                   │
│  ┌─────────┐           │                   │
│  │ SSE     │◀──────────┼── /events ─────────│
│  │ /events │  hw_status│                   │
│  │ 实时推送  │ new_capture│                 │
│  └─────────┘  new_result│                   │
│                         │                   │
│  ┌─────────┐           │                   │
│  │ WS      │◀──────────┼── /ws/video ──────│
│  │ video   │  JPEG帧    │                   │
│  │ canvas   │           │                   │
│  └─────────┘           │                   │
│                         │                   │
│  ┌──────────────────────────────────┐      │
│  │  App 组件 (状态管理中心)          │      │
│  │  ├  mode (当前模型)              │      │
│  │  ├  result (识别结果)            │      │
│  │  ├  health (服务状态)            │      │
│  │  ├  hwStatus (硬件状态)          │      │
│  │  ├  metrics (运行时指标)         │      │
│  │  └  activeTab (当前面板)         │      │
│  └──────────┬───────────────────────┘      │
│             │                               │
│  ┌──────────┴───────────────────────────┐  │
│  │  各面板保持挂载，hidden 显隐切换      │  │
│  │  HardwarePanel │ ModelSelector        │  │
│  │  UploadPanel   │ ResultsDisplay       │  │
│  │  TriggerPanel  │ HistoryList          │  │
│  │  ResultOverlay (ESP32实时结果浮层)     │  │
│  └──────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

**前端关键特性：**

| 模块 | 功能说明 |
|------|----------|
| **侧栏导航** | 220px 左侧导航栏，集成系统状态卡片（服务在线/硬件心跳/当前模型/触发模式/推理队列/错误率），5 秒轮询刷新 |
| **硬件面板** | 四维度硬件状态卡片（连接/采集/活动/固件），实时预览（WebSocket），触发参数调节（距离范围/缓冲时间/触发间隔） |
| **按键测试** | 实时视频流 + 手动拍照识别按钮，直接显示分类结果和置信度 |
| **模型选择器** | 5 模型标签栏（CLIP/豆包/千问/自定义/YOLO），两步确认防误切换，独立 API Key 配置 |
| **上传面板** | 拖拽/点击上传本地图片，自动路由到当前激活模型 |
| **结果展示** | 分类结果 + 置信度仪表 + 垃圾类别说明 + 模型名称 + Top3 候选 |
| **结果浮层** | ESP32 自动触发识别后，页面顶部实时弹出浮动结果卡片，12 秒自动消失 |
| **历史记录** | 服务端历史记录（最多 50 条），按分类筛选，显示触发来源标签 |

### 2.4 系统调试

#### 调试过程概述

本系统经过数个月的迭代开发和持续调试，经历从 v1.0 到 v5.3.0 共计 10 个主要版本的演进。调试工作涵盖固件端、服务端和前端三个层面，以下按调试阶段说明。

#### 第一阶段：固件底层调试

**摄像头色彩校正：** 初期使用 RGB565 像素格式时发现颜色严重失真（红色偏紫、绿色偏蓝）。调试分析发现 OV3660 传感器原生色彩空间为 YUV422，使用 RGB565 时存在字节序转换问题。解决方案：改用 `PIXFORMAT_YUV422` 原生格式配合 `fmt2jpg` 在固件侧完成格式转换，颜色问题彻底解决。

**PSRAM 内存优化：** 初期使用双帧缓冲（`fb_count=2`）时出现内存碎片累积，导致长时间运行后分配失败。分析 ESP32-S3 的 PSRAM 堆特性后，改为单帧缓冲（`fb_count=1`），同时将帧尺寸设为 QVGA（320×240），内存使用从约 300KB 降至约 150KB。

**WiFi 连接稳定性：** 测试中发现路由器重启或信号波动后 ESP32 无法自动重连。解决方案包括：
- 禁用 WiFi 省电模式（`WIFI_PS_NONE` + `WiFi.setSleep(false)`）
- 连接超时延长至 30 秒（60×500ms 重试）
- 启动时先断开残留连接再初始化（`WiFi.disconnect(true, true)` → `WiFi.mode(WIFI_OFF)` → `WiFi.mode(WIFI_STA)`）

**TCP 存活性探测：** 调试中发现 WiFi 连接正常但后端服务不可达时，ESP32 屏幕仍显示"就绪"，用户无法感知服务中断。新增 `probeServerLiveness()` 机制，空闲时每 8 秒 TCP 探测后端 8085 端口，屏幕单独反映后端可达状态。

#### 第二阶段：推理性能调试

**CLIP 推理延迟优化：** 初始版本每次推理都需同时运行图像编码器和文本编码器，延迟约 800-1200ms。调试发现文本标签在运行时固定不变，可采用预计算策略。实现方案：服务启动时一次性对所有 180+ 条文本 prompt 进行编码并 L2 归一化，推理时仅运行图像编码器 + 余弦相似度计算。优化后推理延迟降至 400-600ms（降幅 35-50%）。

**两阶段分类算法调试：** 初始版本对所有 180+ 标签做一次 softmax，结果容易被相似物品干扰（如"苹果"被"橙子"拉低置信度）。调试后设计了两阶段分组分类：
- 第一阶段：将物品按语义分为 16 组（文具、果蔬、厨余、衣物等），组间 softmax 确定物品所属组别
- 第二阶段：组内 softmax 确定具体物品
- 跨组扩展机制：当胜出组与第二名组概率差 < 20% 时合并两组候选
- 低置信度阈值：组内最佳概率 < 20% 时返回"未知物品"
- 优化后分类准确率提升明显，混淆率大幅降低

**服务端并发控制：** 多用户同时请求推理时检测到内存快速增长和响应延迟飙升。引入 `asyncio.Semaphore(INFERENCE_WORKERS * 2)` 实现推理信号量背压，超过队列深度返回 HTTP 429，防止内存耗尽。同时将单一 `state_lock` 拆分为 `_hw_lock`、`_hist_lock`、`_metrics_lock`、`_trigger_lock` 四个独立锁，减少线程竞争约 50%。

#### 第三阶段：通信与可靠性调试

**HTTP 传输优化：** 初始版本 JPEG 质量设为 60，上传体积仍然较大。调试后设为 40，上传体积降低 30-50%，同时开启 `TCP_NODELAY` 降低延迟 50-200ms。固件侧通过 `snprintf` 构建 URL 和 `client.printf()` 直接输出 HTTP Header，消除 8-12 个临时 String 对象，减少堆内存碎片。

**旧帧问题：** 调试中发现每次拍照识别到的画面总是上一帧图像。分析发现 `esp_camera_fb_get()` 返回的是传感器缓冲区中的最新帧，如果缓冲区没有被清空，返回的可能是旧的帧。解决方案：在正式拍照前先调用一次 `esp_camera_fb_get()` + `esp_camera_fb_return()` 丢弃旧帧，再间隔 50ms 等待新帧曝光后进行正式采集。

**SSE 实时推送：** 初始版本前端通过 3 秒轮询获取最新结果，延迟高且浪费带宽。改为 SSE（Server-Sent Events）实时推送后，ESP32 拍照后结果即时到达前端（< 100ms），同时将硬件状态轮询间隔从 3 秒延长至 30 秒做兜底，减少约 20 次 HTTP 请求/分钟。

#### 第四阶段：功能迭代与优化调试

**TOF 距离触发调试：** 添加 TOF 触发后初期出现误触发（空容器经过被检测为物体）和频繁触发（物体在触发范围内轻微移动导致连续触发）。通过三个参数调节优化：
- 距离范围（distance_min/distance_max）：精细化触发区域
- 缓冲时间（cooldown_ms）：物体需要在检测范围内稳定超过此值才触发
- 触发间隔（trigger_interval_ms）：两次触发之间的最小间隔

**标签描述优化（v5.3.0）：** 调试中发现 CLIP 对简短标签（如"笔"）的区分度不够。将 150+ 标签从简短提示词扩展为丰富的视觉描述词（如"a ballpoint pen with a plastic body and metal tip, common stationery"），零样本分类准确率显著提升。

**硬件看门狗（v5.2.0）：** 长期运行测试发现 ESP32 偶发死锁。启用 ESP32 TWDT（Task WatchDog Timer），30 秒超时，在 `loop()` 中定期喂狗，实现系统自动恢复。

#### 调试环境与工具

系统调试使用了以下工具：

| 工具 | 用途 |
|------|------|
| Arduino IDE + 串口监视器 | ESP32 固件开发与调试，输出 Serial 日志 |
| Python Server 日志 | 服务端推理日志、性能指标输出 |
| Chrome DevTools | 前端调试、网络请求分析、性能分析 |
| Wireshark | WiFi 通信抓包分析、TCP 连接分析 |
| Postman | API 接口测试与验证 |
| Git | 版本管理，逐版本回归验证 |

#### 调试关键指标

经过多轮调试优化，系统最终达到以下关键指标：

| 指标 | 初始值 | 优化后 |
|------|--------|--------|
| CLIP 推理延迟 | 800-1200ms | 400-600ms |
| 启动时间 | ~10s | ~6s（并行加载） |
| JPEG 上传大小 | ~80KB | ~40KB |
| SSE 推送延迟 | N/A（轮询 3s） | <100ms |
| 硬件在线检测 | 无 | 30s 心跳 + 60s 超时 |
| HTTP 重试次数 | 1 次 | 3 次（总超时 30s） |
| WiFi 连接超时 | 10s | 30s |

### 2.5 外观设计

#### 设计思路

ESP32-S3 智能垃圾分类终端的外观设计遵循"简洁、直观、易用"的原则：

1. **功能区域明确**：设备正面配备 1.8 寸 TFT 彩色屏幕，清晰显示当前状态（就绪/识别中/分类结果）和垃圾分类信息。
2. **检测区域突出**：摄像头和 TOF 传感器位于同一侧，用户在自然放置物品时正对检测区域，无需刻意对准。
3. **信息层次清晰**：屏幕显示严格按信息优先级排列——最上方显示设备状态图标，中间大字体显示物品名称和分类结果，底部显示置信度。
4. **紧凑一体化**：所有元器件集成在一个紧凑的外壳中，仅需 USB 供电即可独立运行。

#### 屏幕界面展示

**待机界面（就绪态）：**

```
┌──────────────────────────────────┐
│  ◉ TOF 34cm  ■ WiFi -58dBm     │
│                                  │
│        🔄 系统就绪              │
│     ┌──────────────────┐        │
│     │  等待物品放入...   │        │
│     └──────────────────┘        │
│                                  │
│  模式: 距离触发                  │
│  范围: 30-300mm  间隔: 10s      │
└──────────────────────────────────┘
```

**识别过程界面：**

```
┌──────────────────────────────────┐
│  ◉ 距离触发中                    │
│                                  │
│     ┌──────────────────┐        │
│     │   📷 拍照中...    │        │
│     │                  │        │
│     └──────────────────┘        │
│                                  │
│     ⏳ 上传识别中...             │
└──────────────────────────────────┘
```

**分类结果界面：**

```
┌──────────────────────────────────┐
│  ◉ 可回收物                     │
│                                  │
│   ┌──────────────────────────┐  │
│   │        空塑料瓶           │  │
│   │     Confidence: 87%      │  │
│   │  ════════════░░░░░░░░    │  │
│   │  模型: clip              │  │
│   └──────────────────────────┘  │
│                                  │
│  ⌛ 12s 后自动返回待机          │
└──────────────────────────────────┘
```

**具体物品识别流程图示：**

用户将空塑料瓶放置在设备前 → TOF 检测到物体稳定存在 → 自动拍照上传 → 屏幕显示"可回收物 — 塑料瓶 (87%)" → 12 秒后自动返回待机。

**四种垃圾分类结果的屏幕配色方案：**

| 垃圾类别 | 屏幕显示主色 | 图标 | 示例物品 |
|----------|------------|------|----------|
| 可回收物 | 蓝色/青色 | ♻️ | 塑料瓶、纸张、玻璃瓶 |
| 厨余垃圾 | 绿色 | 🍃 | 果皮、剩饭、骨头 |
| 有害垃圾 | 红色 | ⚠️ | 电池、过期药品 |
| 其他垃圾 | 灰色/白色 | 🗑️ | 口罩、餐盒、陶瓷 |

#### 前端 Web 仪表板界面

**左侧导航栏（系统状态总览）：**

```
┌──────────── 220px ────────────┐
│  垃圾分类系统                   │
│  ESP32-S3 智能识别             │
│────────────────────────────────│
│  🔌 按键测试                    │
│  ⚡ 硬件与触发                   │
│  🧠 识别引擎                    │
│  📤 上传与结果                   │
│────────────────────────────────│
│  系统状态                       │
│  ● 服务 在线 · 0.3ms           │
│  ● 硬件 在线 · 128 次采集      │
│  ● 模型 CLIP                   │
│  ● 触发 距离触发                │
│  ● 推理队列 0                  │
│  ● 请求错误率 0.0%             │
│────────────────────────────────│
│  v5.3.0                        │
└────────────────────────────────┘
```

**硬件面板（触发配置与实时预览）：**

- 四张状态卡片：连接状态、采集次数、触发活动、固件版本
- 距离触发参数调节：最小距离（滑块 + 数值）、最大距离（滑块 + 数值）、缓冲时间（滑块 + 数值）、触发间隔（滑块 + 数值）
- 实时摄像头预览（WebSocket 视频流）
- 最新采集缩略图（SSE 自动刷新）

**模型选择器：**

- 5 个标签栏：CLIP / 豆包 / 千问 / 自定义 / YOLO
- 两步确认切换（选中 → 弹窗确认 → 生效），防止误操作
- 云端模型独立配置面板：API Key、Base URL、Model Name 输入框

**测试结果面板：**

- 垃圾分类卡片（分类对应色彩）
- 置信度百分比 + 动画进度条（≥80% 绿 / ≥50% 黄 / <50% 红）
- 物品名称
- 使用模型名称
- Top 3 候选物品列表
- 识别完成时间

---

## 3 总结

### 3.1 项目总结

#### 项目成果

本项目成功设计并实现了一套完整的基于 ESP32-S3 的智能垃圾分类系统，达到了以下成果：

1. **硬件终端**：完成 ESP32-S3 + OV3660 + TOF200C + ST7735 的硬件集成与固件开发，实现距离感应自动触发拍照识别的全自动化工作流，单次识别全流程耗时约 1-1.5 秒。

2. **AI 识别引擎**：集成 CLIP 零样本分类模型（最终替换为豆包向量模型），支持 180+ 标签的两阶段分组分类，自动映射到四类生活垃圾（可回收物/厨余垃圾/有害垃圾/其他垃圾）。同时支持豆包 Vision、千问 Vision、自定义视觉大模型作为云端分类引擎，实现多模型路由与自动回退。

3. **后端服务**：基于 FastAPI 构建完整的后端 API 服务，涵盖分类识别、模型管理、硬件状态监控、历史记录、触发配置等 20+ 个 API 端点。实现 SSE 实时推送、WebSocket 视频流、推理信号量背压、线程安全状态管理、运行时指标统计等高级功能。

4. **前端仪表板**：基于 React 18 + Tailwind CSS 3 构建专业级的 Web 管理界面，提供硬件监控、模型切换、触发配置、结果展示、历史记录等完整功能，通过 SSE 实现实时数据更新。

5. **系统可靠性**：经过多轮优化（TCP 存活性检测、HTTP 重试机制、硬件看门狗、JPEG 质量调优），系统在长期运行测试中表现稳定。

#### 团队分工

本项目由个人独立完成，各模块开发周期如下：

| 阶段 | 时间 | 工作内容 |
|------|------|----------|
| v1.0 | 初期 | ESP32-S3 基础固件（WiFi + 摄像头 + 屏幕）、FastAPI 后端（CLIP 分类 + YOLO 检测） |
| v2.0 | 中期 | OV3660 YUV422 色彩修复、React 前端仪表板（7 个功能模块）、15 个 API 端点、硬件状态监控 |
| v3.0 | 中期 | 视觉大模型集成（豆包/千问/自定义）、5 模型统一选择器、前端模型切换 ESP32 自动跟随、自动回退机制 |
| v4.0 | 后期 | TOF200C 激光测距模块、距离自动触发、触发参数配置 API、前端布局重构 |
| v5.0 | 后期 | SSE 实时推送、摄像头旧帧修复、硬件心跳检测、可观测性中间件、React 18 适配 |
| v5.1-5.3 | 最后优化 | CLIP 文本特征预计算（延迟 -50%）、并行模型加载（启动时间 -50%）、标签描述详细化、TCP 存活性检测、推理信号量背压、锁拆分等 |

#### 问题与解决方案

**问题 1：CLIP 模型推理延迟过高**
- 现象：每次分类耗时 800-1200ms
- 原因：CLIP 文本编码器在每次推理时重复运行
- 解决：服务启动时一次性预计算所有 180+ 条文本 prompt 的特征向量并做 L2 归一化，推理时仅运行图像编码器 + 余弦相似度，延迟降至 400-600ms

**问题 2：摄像头颜色失真**
- 现象：屏幕预览颜色异常，红色偏紫、绿色偏蓝
- 原因：OV3660 传感器原生色彩空间为 YUV422，RGB565 格式存在字节序问题
- 解决：改用 YUV422 原生格式采集，通过 fmt2jpg 在固件侧转为 JPEG 上传

**问题 3：TOF 距离触发误触发**
- 现象：无物体经过时被触发，或物体短暂经过多次触发
- 原因：传感器噪声和检测区域过大
- 解决：引入三重滤波——距离范围限制（min/max）、缓冲时间（物体需稳定存在）、最小触发间隔（防重复触发）

**问题 4：模型分类混淆**
- 现象：相似物品（如"苹果"与"橙子"、"塑料瓶"与"玻璃瓶"）之间经常误判
- 原因：180+ 标签在同一 softmax 空间中竞争，相似类别互相干扰
- 解决：设计两阶段分组分类，先分 16 组确定大类别，再在组内细分类，同时引入跨组扩展机制处理边界模糊情况

**问题 5：WiFi 连接不稳定**
- 现象：长时间运行后 WiFi 断开无法自动重连
- 原因：ESP32 默认 WiFi 省电模式会休眠射频，路由器重启后连接丢失
- 解决：禁用省电模式、延长连接超时至 30s、启动时清除残留连接配置、增加 TCP 存活性探测

**问题 6：SSE 旧帧问题**
- 现象：每次拍照识别结果显示的是上一帧的画面
- 原因：`esp_camera_fb_get()` 返回缓冲区最新帧，而非当前曝光帧
- 解决：拍照前先获取并丢弃一帧旧帧，等待 50ms 让传感器完成新帧曝光后再正式采集

**问题 7：前端状态不一致**
- 现象：不同面板显示的系统状态不同步
- 原因：每个面板各自维护轮询定时器，刷新时机不一致
- 解决：状态提升到 App 组件集中管理，通过 props 下发给各面板；SSE 替代轮询实现实时推送

### 3.2 设计感受

通过本次 ESP32-S3 智能垃圾分类系统的设计实践，我深刻体会到嵌入式 AI 系统开发的复杂性和挑战性。

**从零到一的收获：** 这是我第一次完整经历从硬件选型、电路连接、固件开发、AI 模型集成到前端平台搭建的全链路开发流程。最大的感悟是：一个可用的系统不是各个功能模块的简单堆砌，而是模块间深度耦合与协同优化的结果。例如，CLIP 模型的推理延迟优化不仅仅是算法层面的工作，它还涉及服务端的并发控制、固件的 JPEG 质量设置、HTTP 传输效率等多个环节的配合。

**调试的耐心与系统思维：** 项目中遇到的每个问题几乎都不是单一原因导致的。比如 WiFi 不稳定问题，最终发现是电源噪声、射频省电模式、路由器兼容性、连接超时设置等多因素共同作用的结果。解决此类问题需要系统的排查思路和足够的耐心。

**性能优化的权衡艺术：** 在优化 CLIP 推理延迟时，我尝试了多种方案：模型量化（精度损失）、特征预计算（内存占用增加）、多线程并发（锁竞争风险）。最终选择了特征预计算方案，因为它在不牺牲精度的情况下获得了最显著的性能提升（50%），代价是 ~580MB 的额外内存占用。这种权衡决策让我深刻理解了"没有银弹"的工程哲学。

**物联网的可靠性挑战：** 与纯软件系统不同，物联网系统还需要应对 WiFi 不稳定、传感器噪声、内存碎片、长时间运行性能衰减等硬件层面问题。TCP 存活性检测、硬件看门狗、HTTP 重试退避等机制的引入，让我切身体会到"把系统做可靠"比"把系统做出来"的难度要大得多。

**前端技术的学习：** 虽然本项目的重点是嵌入式系统和 AI，但通过 React 前端仪表板的开发，我学习了现代前端开发的理念：组件化、单向数据流、Hooks 管理状态、SSE/WebSocket 实时通信等。前端开发效率的工具链（Vite 快速构建、Tailwind CSS 原子化样式、Framer Motion 动画）也让我大开眼界。

总的来说，这次项目设计不仅让我巩固了嵌入式系统开发的知识，更让我建立了从硬件到 AI 再到前端的全局视野。ESP32-S3 作为一款高性能、低成本的 AIoT 芯片，在边缘计算场景中展现了巨大的潜力。未来可以考虑将推理也迁移到边缘端（ESP32-S3 内部），实现完全离线的垃圾分类识别，进一步提升系统的实用性和部署灵活性。

---

## 附录

### 附录1 程序

> 完整程序代码请参见项目仓库：
> - **固件主程序**：`firmware/esp32s3_garbage_classifier/esp32s3_garbage_classifier.ino`
> - **后端服务**：`backend/server.py`
> - **模型下载脚本**：`backend/download_models.py`
> - **前端主应用**：`frontend/src/App.jsx`
> - **前端各组件**：`frontend/src/components/*.jsx`
> - **API 聚合层**：`frontend/src/api.js`

#### 固件主程序核心逻辑（伪代码）

```
setup()
  初始化串口
  初始化 ST7735 屏幕 (软件 SPI: CS=40, DC=39, RST=1, MOSI=38, SCLK=14)
  初始化 OV3660 摄像头 (YUV422, QVGA, PSRAM)
  初始化 TOF200C VL53L0X (I2C: SDA=47, SCL=21)
  连接 WiFi (禁用省电模式, 30s 超时)
  首次拉取触发配置
  进入就绪状态

loop()
  // 定时任务
  if (millis() - lastLivenessProbe > 8000)
     探测后端 TCP 可达性
  
  if (millis() - lastHeartbeatMs > 30000)
     发送心跳 /hardware/heartbeat
  
  if (millis() - lastConfigFetch > 30000)
     拉取触发配置 /trigger/config

  // TOF 距离检测
  距离 = readTOF()
  if 距离在 [distanceMin, distanceMax] 范围内:
    if 第一次检测到物体:
      presenceStart = millis()
    else if millis() - presenceStart > cooldownMs:
      if millis() - lastTrigger > triggerIntervalMs:
        captureAndClassify()
        lastTrigger = millis()
  else:
    presenceStart = 0

captureAndClassify()
  // 丢弃旧帧
  丢弃一帧旧帧 (esp_camera_fb_get + fb_return)
  delay(50)
  
  // 拍照
  fb = esp_camera_fb_get()
  fmt2jpg(fb->buf → jpgBuf)  // YUV422 转 JPEG
  
  // 上传
  POST /classify?source=esp32 (multipart/form-data)
  最多重试 3 次
  
  // 解析并显示结果
  parse JSON → 获取 item_label_zh, waste_category_zh, confidence
  tft 显示分类结果 (12s 后自动返回待机)
```

#### 后端 CLIP 分类核心逻辑

```
_classify_clip(image_data):
  image = Image.open(image_data).convert("RGB")
  pixel_values = processor(images=image).pixel_values.to(device)
  
  // 图像特征提取
  img_feats = model.get_image_features(pixel_values)
  img_feats = L2_normalize(img_feats)
  
  // 相似度计算
  logits = img_feats @ text_features.T * logit_scale
  
  // 两阶段分组分类
  // Stage 1: 组级 softmax
  for each group in 16 groups:
    组 logit = 组内标签的最大 logit
  组概率 = softmax(组 logits)
  胜出组 = argmax(组概率)
  
  // 跨组扩展
  if 胜出组概率 - 第二名组概率 < 20%:
    合并两组标签
  else:
    仅胜出组标签
  
  // Stage 2: 组内 softmax
  组内概率 = softmax(组内标签 logits)
  best = argmax(组内概率)
  if best_conf < 20%:
    return "未知物品"
  
  // 垃圾分类映射
  分类 = WASTE_CATEGORY_MAP[best_label]
  
  return { waste_category, item_label_zh, confidence, top3, model_used }
```

### 附录2 设计图纸

> 由于本设计报告以文本形式呈现，电路连接图已在第 2.2 节中以 ASCII 示意图形式展示。以下是各原理图的索引：

**图纸列表：**

1. **系统架构框图** — 见第 2.1 节"系统整体设计框图" ↑
2. **ST7735S 屏幕接线图** — 见第 2.2 节 ↑
3. **TOF200C 模块接线图** — 见第 2.2 节 ↑
4. **OV3660 摄像头接线图** — 见第 2.2 节 ↑
5. **固件程序流程图** — 见第 2.3 节 ↑
6. **两阶段分类算法流程图** — 见第 2.3 节 ↑
7. **前端数据流架构图** — 见第 2.3 节 ↑
8. **屏幕界面设计图** — 见第 2.5 节 ↑

> **注：** 完整的设计图纸（电路原理图 PDF、PCB 布局图、3D 装配图）以独立文件形式提交，请参见项目目录中的 `docs/schematics/` 文件夹。
